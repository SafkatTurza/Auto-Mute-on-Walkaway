[package]
name = "amow-domain"
description = "Core domain: presence state machine, events, value objects."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
